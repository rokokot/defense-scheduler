## Copy this file to `.env` and customize it for your deployment.
## Do NOT commit a file named `.env`—it is ignored by git.

# Host that should be used when the frontend calls the API (e.g., `localhost` for dev or the instance IP).
API_HOST=localhost

# Full API URL exposed to the frontend bundle. Set this to your public backend (e.g., `http://134.98.159.147:8000`).
VITE_API_URL=http://localhost:8000

# Comma-separated list of additional frontend origins to whitelist via CORS (e.g., `134.98.159.147`).
# Each entry may be a bare host, host:port, or full URL.
FRONTEND_HOSTS=

# Optionally override the entire CORS whitelist (comma-separated origins). If set,
# FRONTEND_HOSTS is ignored.
ALLOWED_ORIGINS=
